name: My First Workflow

on: push

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print a message
        run: echo "Hello, this is my first GitHub Actions workflow!"

      - name: Print another message
        run: echo "Today's date is:" 
      - run: echo $(date)
